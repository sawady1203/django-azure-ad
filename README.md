# django-azure-ad-b2c
DjangoとAzure AD B2Cを連携するためのリポジトリ

```
$ python -m venv venv
$ source ./venv/bin/activate
(venv)$ python -m pip install -U pip
(venv)$ python -m pip install -r requirementxts.txt
```
